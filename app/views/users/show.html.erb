<p>
  <strong>Username:</strong>
  <%= @user.username %>
</p>

<h3>Articles</h3>
<%= render 'articles/article', articles: @articles %>
<%= will_paginate @articles, renderer: custom_paginate_renderer, page_links: false  %>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
